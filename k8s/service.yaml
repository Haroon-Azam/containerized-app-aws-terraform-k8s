apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio-service
spec:
  type: NodePort
  selector:
    app: devops-portfolio-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080